Quick script for returning header and whitespace stripped character count from a fasta file. 

Whitespace issues can be pretty confounding for an amateur researcher, something like this could be useful as a refresher/reference. 
